# Collector service environment example
# Copy to `collector/.env` for local development

# Port to serve collector on
COLLECTOR_PORT=8000

# Directory where events and sites are stored (relative to service root)
DATA_DIR=./data

# CORS allowed origins (comma-separated list)
# Add your deployed frontend and backend URLs here for production
# Example: COLLECTOR_ALLOWED_ORIGINS=https://your-frontend.onrender.com,https://your-backend.onrender.com,http://localhost:5173
COLLECTOR_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:8010
