## Example env for demo auth service (auth/demo)
# Copy to `.env` for local development and adjust secrets before running.

# Port the auth service listens on
DEMO_AUTH_PORT=8001

# HMAC secret used to sign session tokens (change in production)
DEMO_HMAC_SECRET=change-me-to-a-secure-random-string

# Credential expiry (hours)
CREDENTIAL_EXPIRY_HOURS=1

# Token expiry (minutes)
TOKEN_EXPIRY_MINUTES=25

# CORS allowed origins (comma-separated list)
# Add your deployed frontend and backend URLs here for production
# Example: AUTH_ALLOWED_ORIGINS=https://your-frontend.onrender.com,https://your-backend.onrender.com,http://localhost:5173
AUTH_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:8010
